{
  "_comment": "Bot defaults live in BotConfig.cs and are overridden by env.local. Only Serilog config is set here.",
  "Serilog": {
    "MinimumLevel": "Information",
    "WriteTo": [
      { "Name": "Console" },
      { "Name": "File", "Args": { "path": "logs/bot-.log", "rollingInterval": "Day", "retainedFileCountLimit": 14 } }
    ]
  }
}
